/*///////////////////////////////////////
///////////////////////////////////////
 12) Receba K numeros inteiros quaisquer, 
K representa a quantidade de termos solicitada pelo usuario e cada termo pode ser representado pela variavel N. 
Exiba a quantidade de numeros positivos recebidos e a me'dia dos numeros pares.
C CODE*/

#include <stdio.h>

int calcula(int ktamanho){
	int num[ktamanho]
	int i = 0;
	int mediapares = 0;
	int positivos = 0;
	
	for(i = 0; i < ktamanho ; i++ ){
		printf("Digite o %do número ", i);
		scanf("%d", num[i]);

		if((num[i] % 2) = 0){

		}
	}
}


int main (){
int ktam = 0;

	printf("Digite o número base: \n");
	scanf("%d", &ktam);

	calcula(ktam);
}